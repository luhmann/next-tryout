import React from 'react'
import styled from 'styled-components'

import { MAIN_FONT, COLORS } from '../lib/styleConstants'

const BlogParagraph = styled.p`
  font-family: ${MAIN_FONT};
  font-size: 12px;
  line-height: 1.3;
`

export default BlogParagraph
